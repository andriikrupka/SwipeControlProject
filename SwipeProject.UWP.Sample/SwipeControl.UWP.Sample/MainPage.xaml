<Page
    x:Class="SwipeControl.UWP.Sample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SwipeControl.UWP.Sample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:SwipeControl"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:SwipeControl Background="Aquamarine"
                               SwipeMode="LeftRight"
                               VerticalAlignment="Center"
                               VerticalContentAlignment="Center">

            <controls:SwipeControl.LeftPanel>
                <Grid Width="150"
                      Background="Red">

                </Grid>
            </controls:SwipeControl.LeftPanel>

            <controls:SwipeControl.RightPanel>
                <Grid Width="150"
                      Background="Orange">

                </Grid>
            </controls:SwipeControl.RightPanel>

            <Grid HorizontalAlignment="Stretch"
                  Background="Black">
                <TextBlock Text="your content"
                           HorizontalAlignment="Center"
                           Foreground="Wheat"
                           FontSize="55"/>
            </Grid>
        </controls:SwipeControl>
    </Grid>
</Page>
